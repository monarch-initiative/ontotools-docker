# Used by ols4-dataload container (compose maps it to environment: - JAVA_OPTS=${JAVA_OPTS})
JAVA_OPTS=--add-modules jdk.incubator.vector --add-opens=java.base/java.nio=ALL-UNNAMED -Xms12g -Xmx12g

# Apply to ALL Java processes inside the dataload container (rdf2json, neo4j-admin import, etc.)
JAVA_TOOL_OPTIONS=-Xms12g -Xmx12g